# split()
É um método do [[JavaScript]], utilizado para separar [[string]], pode ser atribuído algum caractere para fazer essa separação. Veja o exemplo:
```
const times = "Avaí, Figueira, Vasco, Cruzeiro";
const time = times.split(",");
```
